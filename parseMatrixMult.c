#include <stdio.h>
#include <stdlib.h>

int multiply(int[] a, int[] b) {
    //Cij += Aik * Bkj
}

int main(void) {
    int row, col, terms;
    scanf("%d %d %d", &row, &col, &terms);
    int* a = malloc(sizeof *a * terms);
}
